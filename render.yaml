databases:
  - name: helpjuice
    databaseName: helpjuice
    user: helpjuice

services:
  - type: web
    name: helpjuice
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: helpjuice
          property: connectionString
      - key: 3cbb10d3554ad425bea771655307fe9a
        sync: fals